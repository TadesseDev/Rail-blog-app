<div id="posts">
  Post: <%= @post.id%> BY <%= @post.user.name%>
  <h1><%= @post.title %></h1>
  <div class="post-stats">
    Comments: <%= @post.commentsCount %>,
    Likes: <%= @post.likesCount %>
  </div>
  <p>
    <%= @post.text %>
  </p>
  <div id="post-comments">
    <% @comments=(@post.comments.includes(:user).order(created_at: :ASC))%>
    <%@comments.each do |comment|
      %>
    <%= comment.user.name%> : comment <%=comment.id%>
  <%end%>
</div>
<%= form_with  url: user_post_likes_url(@post.user, @post), local: true, scope: :scop do |form|%>
  <%= form.submit value: 'like'%>
<% end %>
<a  href="/users/<%= params[:user_id]%>/posts/<%= @post.id%>/comments/new"> Add comment
</div>
